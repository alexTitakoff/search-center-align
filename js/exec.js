setInterval(() => {
    // alert('align')   
    
    if (window.location.hostname === "yandex.ru") {
    //    alert(document.querySelector('.home-arrow__tabs').innerText) 
       alert('It yandex') 
    }
}, 1000)